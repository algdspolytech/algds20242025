#include "treap.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

//int main() {
//    srand(time(NULL));
//    Node* root = NULL;
//
//    root = insert(root, create_node(1, 10));
//    root = insert(root, create_node(3, 20));
//    root = insert(root, create_node(5, 30));
//
//    printf("Sum [2, 4): %d\n", get_sum(root, 2, 4));
//    free_tree(root);
//    return 0;
//}